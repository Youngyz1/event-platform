# supabase/migrations — INCOMPLETE HISTORY (C3)

This directory contains only `20260828231400_migration_71_follows_privacy.sql`
(content-identical to `db/migration_71_follows_privacy.sql`).

It is NOT the canonical history. The canonical migration chain lives in
`db/migration_01_*.sql` … `db/migration_71_*.sql` — see
`docs/database-migrations.md`.

Do not run `supabase db push` expecting a full schema from this directory, and
do not add new one-off files here without either backfilling 01–70 or
documenting the CLI workflow that replaces `db/`.
